// add solution here
function theBeatlesPlay(musician, instrument) {
  let array = []
  for ( let i = 0; musician.length ; i++) {
    array.push(`${musician} plays ${instrument}.`)
  }
  return array
}
